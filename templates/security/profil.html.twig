{% extends "base.html.twig" %}

{% block title %} {{ parent()}} | Profil {% endblock %}

{% block h1 %}Profil{% endblock %}

{% block body %}

{% for notification in app.flashes('success') %}
    <div class="alert alert-success m-3 col-md-6 mx-auto">{{notification}}</div>

{% endfor %}
<ul class="list-group">

    
    <li >Email: {{ app.user.email  }}</li>
    
</ul>    
    
    <div class="row justify-content-between">
        <a href="{{ path('profil_modification') }}" class="btn btn-danger col-md-4">Modifier le profil</a>
        
    </div>





{% endblock %}
